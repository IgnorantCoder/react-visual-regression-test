import styled from "styled-components";

export default styled.div`
    display: flex;
    :before {
        content: "GOOD\\ATHRU";
        white-space: pre-wrap;
        font-size: 0.4rem;
        line-height: 0.4rem;
        color: #00acc1;
        padding-right: 3px;
        margin: auto 0;
        font-weight: 700;
    }
`;
